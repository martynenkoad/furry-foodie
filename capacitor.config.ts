import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'furryfoodie.com',
  appName: 'furry-foodie',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
